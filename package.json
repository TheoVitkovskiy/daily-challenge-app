{
  "name": "daily-challenge-app",
  "version": "0.0.1",
  "description": "A web application for daily challenges.",
  "engines": {
    "node": "6.9.4"
  },
  "scripts": {
    "start": ".react-ui/node_modules/.bin/babel-node server.js",
    "heroku-postbuild": "cd react-ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "express": "4.14.1"
  },
  "devDependencies": {}
}
